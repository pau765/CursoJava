     System.out.println("Crear una tabla de longitud 10 "
                + "que se inicializara con numeros aleatorios comprendidos "
                + "entre 1 y 100. Mostrar la suma de todos los numeros"
                + " aleatorios que se guardan en la tabla.");
        int numvalores[] = new int [10];
int numvalores[] = new int [10];
        for (int i = 0; i < numvalores.length; i++) {
            numvalores[i] = (int) (Math.random() * 100 +1);
        }
        int suma = 0;
        for(int valor : numvalores) {
            suma += valor;
        }
        System.out.println("La suma de los valores aleatorios es " + suma);
          System.out.println("La suma de los valores aleatorios es " + suma);
